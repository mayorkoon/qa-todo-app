{
  "name": "qa-todo-app",
  "scripts": {
    "install-all": "npm --prefix backend install && npm --prefix frontend install && npm --prefix test/cypress install",
    "start": "concurrently \"npm --prefix backend start\" \"npm --prefix frontend start\"",
    "test": "npm --prefix test/cypress run test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
